#!/usr/local/bin/fontforge

scale  = Strtol($1);
suffix = $2;

i = 3;

while (i < $argc)

  Print("scaling ", $argv[i], "...");

  Open($argv[i]);

  SetFontNames($fontname + suffix);

  SelectAll();

  Scale(scale, 100, 0, 0);

  pfb = $argv[i]:r + suffix + ".pfb";

  Generate(pfb, "", 1);

  i++
endloop

